# Optional dependencies for SVG Architect Post-processing
# Use: pip install -r requirements.txt

cairosvg>=2.7.1
resvg-py>=0.1.0
svgo>=0.1.0  # If you want to use python-based svgo-like optimization

# QA Preview Renderer (render_svg_preview.py)
# Primary backend: playwright (then run: playwright install chromium)
playwright>=1.40.0
# Fallback backend: cairosvg (requires libcairo native library)
# Image analysis
Pillow>=10.0.0
numpy>=1.24.0
